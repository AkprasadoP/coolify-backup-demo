services:
  app:
    image: nginx:alpine
    ports:
      - "8080:80"
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: demopass
      POSTGRES_DB: demodb
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
